# Core dependencies
python-dotenv>=0.21.0
colorama>=0.4.6
pydantic>=2.7.1
tqdm>=4.66.4

# Audio processing
PyAudio==0.2.14
SpeechRecognition==3.10.4
pygame>=2.5.2
sounddevice>=0.4.6
soundfile>=0.12.1
pydub>=0.25.1

# Image processing (for GIF-based face animations)
Pillow>=10.0.0

# macOS Native Voice Isolation (requires macOS 12.0+)
pyobjc-core>=10.0; platform_system == "Darwin"
pyobjc-framework-AVFoundation>=10.0; platform_system == "Darwin"
pyobjc-framework-CoreAudio>=10.0; platform_system == "Darwin"
pyobjc-framework-CoreML>=10.0; platform_system == "Darwin"

# Wake word detection
pvporcupine>=2.2.0  # For "Hey Howdy" wake word detection

# Networking and API clients
requests>=2.31.0   # Used for FastWhisper API and ESP32 LED Matrix
urllib3>=2.2.1
httpx>=0.27.0

# AI models
openai>=1.30.1
groq>=0.4.0
deepgram-sdk>=2.11.0
ollama>=0.1.0     # Local LLM support
elevenlabs>=0.2.26
cartesia>=0.3.0

# Web server (for FastWhisperAPI)
fastapi[standard]>=0.100.0
uvicorn>=0.23.0

# Data processing
numpy==2.2.5  # Fixed version for compatibility with ONNX Runtime
scipy>=1.10.0
nltk>=3.9.1
joblib>=1.4.2
regex>=2021.8.3

# ONNX for Kokoro TTS
# For non-Apple Silicon platforms
onnxruntime>=1.16.0; platform_machine != "arm64" or platform_system != "Darwin"
# For Apple Silicon (M-series) Macs - use regular onnxruntime (silicon is deprecated)
onnxruntime>=1.16.0; platform_machine == "arm64" and platform_system == "Darwin"
torch>=1.13.0
torchaudio>=2.0.0  # For Silero VAD neural network voice detection
kokoro-onnx==0.4.8  # TTS engine for voice synthesis with Apple Silicon support

# Optional: keyboard for hotkeys
keyboard>=0.13.5

# Wireless ESP32P4 device support
opuslib>=3.0.1  # OPUS audio codec for wireless streaming
websocket-client>=1.0.0  # WebSocket communication with devices